# Report Assets

This directory is the single shared image folder for:

- `../main.tex`
- `../slides.tex`

It contains all logos, photos, and generated benchmark charts needed to compile
the report and the Beamer slides on another machine.

Do not move individual images out of this folder unless the LaTeX
`\graphicspath` or `\includegraphics` paths are updated accordingly.
